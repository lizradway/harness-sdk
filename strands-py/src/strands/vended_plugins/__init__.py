"""Vended plugins for Strands agents.

Included plugins:
    - ``ContextInjector`` — fold just-in-time text into the model input.
    - ``ContextOffloader`` — offload conversation context to external storage.
    - ``GoalLoop`` — iterative refinement against a validator.
    - ``AgentSkills`` — AgentSkills.io integration with progressive disclosure.
    - ``Steering`` — steer agent behavior with runtime directives.
    - ``Todos`` — structured task list the agent maintains for multi-step work.
"""
